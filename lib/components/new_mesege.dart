import 'package:flutter/material.dart';

class New_meseges extends StatelessWidget {
  const New_meseges({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Nova Mensagem'),
    );
  }
}